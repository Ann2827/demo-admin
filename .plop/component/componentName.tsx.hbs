import * as React from 'react';

import { Box } from '@mui/material';
import { {{name}}Props } from './{{name}}.types';

const {{name}}: React.FC<{{name}}Props> = ({ sx, ...rest }) => {

  return (
    <Box sx={sx}>{{name}}</Box>
  );
};

export default {{name}};
