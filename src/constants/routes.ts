import { withSlash } from '@/utils/url';

export const ROUTE_MAIN = '/';
export const ROUTE_LOGIN = withSlash('login');

export const ROUTE_DASHBOARD = withSlash('dashboard');
export const ROUTE_POSTS = withSlash('posts');
export const ROUTE_USERS = withSlash('users');
export const ROUTE_TASKS = withSlash('tasks');
